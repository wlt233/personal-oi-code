#include <iostream>
#include <algorithm>
#include <iomanip>
using namespace std;
#define ll long long

int n, t;
bool cho[11][101];

int main()
{
    //freopen("shirt.in","r",stdin);
	//freopen("shirt.out","w",stdout);

    cin >> t;
    for (int time = 1; time <= t; time++) {
        cin >> n;
    }
   memset(cho[i])


   
    //fclose(stdin);
    //fclose(stdout);


}